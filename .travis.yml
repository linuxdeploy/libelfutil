language: c++
os: linux

addons:
  apt:
    packages:
    - cmake
    - libboost-filesystem-dev
    - libboost-regex-dev

notifications:
  email: false

git:
  depth: 3

script:
  - cmake .
  - make -j$(nproc)
  - ctest -V
